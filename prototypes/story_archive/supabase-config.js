export const STORY_ARCHIVE_SUPABASE = {
  enabled: false,
  url: "",
  anonKey: "",
  table: "stories",
  includeSeedStories: true
};

// Fill `url` and `anonKey`, then switch `enabled` to `true`.
// Use the Supabase publishable / anon key here.
// Never put your service_role key in frontend code.
